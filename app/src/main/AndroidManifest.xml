<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andersonroman.sanvicenteturistico">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/images"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- ++++++++++++++++++++++++++++comentario++++++++++++++++++++++++++++ -->
        <activity android:name=".SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".DemoActivity" />
        <activity
            android:name=".HotelesActivity"
            android:label="@string/title_activity_hoteles"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListaActivity" />
        <activity
            android:name=".HotelesDrawerActivity"
            android:label="@string/title_activity_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".BaresActivity"
            android:label="@string/title_activity_bares"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SitiosTuristicosActivity"
            android:label="@string/title_activity_sitios_turisticos"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".BaresDrawerActivity"
            android:label="@string/title_activity_bares_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SitiosDrawerActivity"
            android:label="@string/title_activity_sitios_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainDrawerActivity"
            android:label="@string/title_activity_main_drawer"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PerfilDrawerActivity"
            android:label="@string/title_activity_perfil_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ListaDrawerActivity"
            android:label="@string/title_activity_lista_drawer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProductosDrawerActivity"
            android:label="@string/title_activity_productos_drawer"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>